name: flutter_navigation_bottom
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  charts_flutter: ^0.12.0
  flutter_osm_plugin: any
  flutter:
    sdk: flutter
  latlng: ^2.0.5
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  group_button: ^5.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
